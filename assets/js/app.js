(function($) {

    $(function() {
        const game = {
            startModal() {
                $('#startGame').modal('show');  // Open the Bootrap 4 modal on start
            }
        }
        game.startModal();
    })
})(jQuery);